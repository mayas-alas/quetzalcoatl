schema = 1

[mesh]
# Ambos nombres soberanos requeridos son válidos:
#   https://headscale.node.gnx
#   https://controlplane.node.gnx
controller_url = "https://controlplane.node.gnx"
expected_domain = "node.gnx"
# Opcional cuando el DNS del host todavía no conoce el controller. GNX usa
# estas IP sólo para los aliases configurados y conserva TLS/SNI por hostname.
bootstrap_addresses = []

[runtime]
machine_name = "quetzalcoatl"
profile = "lab"

[services]
garage = false
forgejo = false
runner = false
