# Quetzalcoatl documentation map

This directory contains the product contract, not the execution tracker.

## Current sources

- `ARCHITECTURE.md` is the normative technical contract for the scoped PoC/MVP.
- `VALIDATION.md` is the reproducible Dockur and physical-lab acceptance runbook.
- `../quetzalcoatl-cierre-poc-mvp.md` is the closure checklist while its remaining consistent requirements are folded into the architecture.
- `../.AGENTS/SCOPE.md` fixes the release boundary.
- `../.AGENTS/TRACKER.md` is the only active progress and score tracker.
- `../.AGENTS/EVIDENCE.md` is the accepted evidence ledger.

## Legacy routes

- `../PoC.md` is a short pointer for the original exploratory specification.
- `TRACKING.md` is a short pointer for the retired tracker.

Do not restore commands, features, or acceptance claims from a legacy document unless they also exist in the current architecture and scope.
