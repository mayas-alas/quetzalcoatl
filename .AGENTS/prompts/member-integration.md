You are the INT-01 member integration worker. Do not spawn subagents. Read C:\Users\mayas\Quetzalcoatl\AGENTS.md, C:\Users\mayas\Quetzalcoatl\.AGENTS\SCOPE.md, C:\Users\mayas\Quetzalcoatl\.AGENTS\DECISIONS.md, C:\Users\mayas\Quetzalcoatl\.AGENTS\tasks\MEMBER_INTEGRATION.md, and the complete current diffs in this worktree. The worktree already contains the architect-reviewed CORE-01, CORE-02, and PVE-01 baseline; do not rewrite those designs or edit the payload/manifest.

Implement only the runtime/state integration described by INT-01. The installed payload contract is exactly five newline-delimited stdin values to `gnx-pve-cluster-create join` (controller IP, controller hostname, member IP, member hostname, controller PVE password) and exact success stdout `PVE_JOIN=ready`. Preserve the controller path byte-for-byte except where the explicit role guard requires a narrow signature change. No secret may enter an error, test fixture output, argv, state, or status.

Run cargo fmt --all -- --check, cargo clippy -p gnx-service -- -D warnings, cargo test --workspace, the payload manifest test, and git diff --check. Do not commit, push, dispatch, or mutate remote state. Return changed files, exact checks, remaining live-test blockers, and any contract conflict instead of broadening scope.
