You are correcting the current uncommitted CI-01 work after architect review. Do not spawn subagents. Read C:\Users\mayas\Quetzalcoatl\.AGENTS\SCOPE.md, C:\Users\mayas\Quetzalcoatl\.AGENTS\DECISIONS.md, C:\Users\mayas\Quetzalcoatl\.AGENTS\tasks\CI_DOCKUR.md, the original workflow via `git show HEAD:.github/workflows/windows-rdp-tailscale.yml`, and the real GNX CLI at C:\Users\mayas\Quetzalcoatl\crates\gnx-cli\src\main.rs. Work only in this harness repository.

The official noVNC/Windows implementation is https://github.com/dockur/windows. Treat it as the runtime source of truth; do not modify or push to that upstream repository. The local GitHub harness only orchestrates its pinned image.

The current low-reasoning diff is not acceptable. Correct it with this bounded design:

1. Use `permissions: contents: read`. Never claim draft release download needs write permission.
2. Never invoke invented commands. The installed CLI supports only `gnx status`, `gnx status --json`, and interactive `gnx configure`; there is no `--version`, `runtime status`, or `cluster probe` command.
3. Do not automate or pipe `gnx configure`: secret entry deliberately requires an interactive Windows console. Treat the Dockur lane as a real consumer-style interactive compatibility lab. The workflow downloads the frozen setup asset, verifies its SHA-256 on the Linux host, and makes it available in the guest for the operator/architect to run through noVNC. It must not claim runtime convergence until genuine `gnx status --json` evidence is collected.
4. Make authenticated noVNC the primary bounded access mode. Dockur officially exposes its web viewer on container port 8006; bind it only to host loopback and proxy it through Tailscale Serve with HTTPS, no Funnel. Keep optional loopback RDP only if useful. Provide an explicit, bounded session duration and clean up afterward. It is acceptable to show the tailnet-authenticated HTTPS noVNC URL in the job summary; never print passwords or auth keys.
5. Keep controller/member-1/member-2 as deterministic logical slots, but clearly state that separate ephemeral runs are compatibility sessions and cannot prove quorum or Corosync. Do not implement a fake cluster probe or derive network acceptance from the runner edge. DERP or RTT >=5 ms remains a physical/GNX cluster acceptance failure when actual member evidence exists, not something this workflow may fabricate.
6. Place user-invoked helpers beside the installer without naming an automatic Dockur `install.bat`. Provide narrow helpers for elevated install/repair/uninstall and a redacted evidence collector that uses only real commands/service queries and writes to Dockur's documented shared Z: drive. The evidence collector may capture installer SHA, service state/identity, installed binary hashes, and exact `gnx status --json`; it must never capture configuration inputs, auth keys, passwords, or raw logs. If GNX is not installed/configured, report that honestly.
7. Preserve the existing pinned Dockur image digest and fail-closed KVM API 12, nested virtualization, TUN, memory, and disk checks. Wait for the web viewer/container health without equating viewer availability with GNX acceptance.
8. Upload only redacted short-lived evidence plus bounded host diagnostics. Do not delete the historical native workflow and do not make it a required signal.
9. README must document the exact interactive sequence: open authenticated noVNC, run the frozen installer as a user, allow real Windows reboots, run `gnx configure` interactively, run the evidence helper, and interpret `gnx status --json`. Explain that official Dockur `/oem/install.bat` is intentionally not used because the product's configuration is interactive and this lane is meant to exercise the consumer flow.

Validate YAML and embedded Bash/PowerShell as far as this host permits; use available local tools or bundled Python rather than claiming none exist without checking. Run git diff --check. Do not commit, push, dispatch, expose a session, or mutate remote state. Return a concise completion report and remaining remote-test blockers.
