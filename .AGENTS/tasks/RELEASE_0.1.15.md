# Release 0.1.15 — recovery-first member release

Deliver a bounded release that closes the observed dependency-MSI path failure, adds local CLI version reporting, and makes the existing first-member join observable and confirmable.

Acceptance requires stable dependency staging, a bounded reboot/rerun journal, no direct WSL/Podman Burn MSI execution, exact version flags, explicit member phases, one-controller discovery with multiple members, unchanged public protocol/state schemas and a clean source archive.
