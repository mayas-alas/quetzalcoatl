version = 1

[mesh]
# El primer corte conecta este nodo a un control plane ya existente.
# El ejemplo privado requiere DNS y CA preparados antes de conectar.
control_server = "https://mesh.gnx"

# Identidades, claves y tokens nunca viven en este archivo.
