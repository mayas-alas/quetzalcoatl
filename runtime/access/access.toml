# Dedicated WSL distribution name; this must already exist on the host.
distribution = "gnx-pihole"
zone = "mesh.gnx"
wildcard = true
state_dir = "/var/lib/gnx/access"
uplink = "eth0"
uplink_mtu = 1500
