[package]
name = "gnx-protocol"
version = "0.1.1"
edition.workspace = true

[dependencies]
serde = { version = "1.0", features = ["derive"] }
zeroize = { version = "1.8", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0"
