# Working contract

Work directly without subagents. Read docs/business-requirements.md and the
relevant section of docs/architecture.md. Current evidence is in
docs/release-validation.md; older checkpoints are historical.

Preserve the existing GNX deployment. Test in explicitly named isolated instances.
Proxmox (Dockurr), Quadlet and https://proxmox.gnx are required. Use one Linux
runtime core for native Linux and the dedicated-account Windows WSL service.
Do not claim final acceptance until docs/poc.md G0-G6 have executable evidence.
Never commit credentials, private state, or local orchestration recipes.
